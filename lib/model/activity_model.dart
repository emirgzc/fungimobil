class ActivityModel {
  final String imageUrl;
  final String title;
  final String location;
  final double price;
  final int quota;
  final int currentSavedPerson;

  ActivityModel(this.imageUrl, this.title, this.location, this.price, this.quota, this.currentSavedPerson);
}